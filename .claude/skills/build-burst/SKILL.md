---
description: "Burstビルド（最大最適化）"
allowed-tools:
  - "Bash(tools\\@build.cmd:*)"
---

Burstビルドを実行してください。

**実行コマンド**:
```
powershell -NoProfile -Command "Set-Location 'C:\Users\nanat\Desktop\NS-ENGINE'; & cmd /c 'tools\@build.cmd Burst' 2>&1"
```

timeout: 300000ms
